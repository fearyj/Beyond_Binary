<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Top bar (back arrow) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/post_detail_bar_height"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="4dp"
        android:paddingEnd="12dp"
        android:background="#FFFFFF">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="@dimen/post_detail_back_button_size"
            android:layout_height="@dimen/post_detail_back_button_size"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back"
            android:scaleType="centerInside"
            app:tint="#000000" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Post"
            android:textSize="@dimen/text_size_body_post_title"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Post Header -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="@dimen/header_height"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="12dp"
                android:paddingEnd="12dp">

                <!-- Profile pic -->
                <ImageView
                    android:id="@+id/header_profile_pic"
                    android:layout_width="@dimen/post_detail_header_profile_pic"
                    android:layout_height="@dimen/post_detail_header_profile_pic"
                    android:background="@drawable/profile_circle_background"
                    android:scaleType="centerCrop" />

                <TextView
                    android:id="@+id/post_username"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="10dp"
                    android:textSize="@dimen/text_size_body_chat_btn"
                    android:textStyle="bold"
                    android:textColor="#000000" />

                <!-- Three dots menu -->
                <ImageButton
                    android:id="@+id/btn_more"
                    android:layout_width="@dimen/post_detail_header_profile_pic"
                    android:layout_height="@dimen/post_detail_header_profile_pic"
                    android:src="@android:drawable/ic_menu_more"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="More options"
                    android:scaleType="centerInside"
                    app:tint="#000000" />

            </LinearLayout>

            <!-- Image Carousel -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/image_carousel"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/post_detail_carousel_height"
                    android:background="#F5F5F5" />

                <!-- Slide counter badge -->
                <TextView
                    android:id="@+id/slide_counter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="top|end"
                    android:layout_margin="12dp"
                    android:background="@drawable/bg_slide_counter"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:text="1/5"
                    android:textColor="#FFFFFF"
                    android:textSize="@dimen/text_size_body_small" />

            </FrameLayout>

            <!-- Dot indicators -->
            <LinearLayout
                android:id="@+id/dot_indicators"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:paddingTop="10dp"
                android:paddingBottom="4dp" />

            <!-- Caption -->
            <TextView
                android:id="@+id/caption_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingBottom="6dp"
                android:textSize="@dimen/text_size_body_default"
                android:lineSpacingExtra="3dp"
                android:textColor="#1E2939" />

            <!-- Timestamp -->
            <TextView
                android:id="@+id/timestamp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingTop="4dp"
                android:paddingBottom="16dp"
                android:text="2 hours ago"
                android:textSize="@dimen/text_size_caption"
                android:textColor="#8E8E8E" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>
